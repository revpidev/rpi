//! Transcript rendering tables (glyphs/colors/action prefixes) and the
//! renderCall/renderResult component builders.
//!
//! **TE34 skeleton only** — the glyph/color tables, `renderTodoCall` /
//! `renderTodoResult` component trees, and the overlay row assembly land
//! with TE35 (upstream `view/format.ts` @ `0fdf4f8`). The module exists so
//! the P0 crate layout matches plugin 02-design §2 and the module mapping
//! stays reviewable file-by-file.
