[package]
name = "pir-wasm-spike-guest"
description = "T02 Wasm ABI spike guest module (built to wasm32-unknown-unknown)"
version = "0.1.0"
edition = "2021"
license = "MIT"

# Standalone crate: not part of the pir workspace (different build target).
[workspace]

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
strip = true
