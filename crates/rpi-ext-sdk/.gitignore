# Workspace member since v0.1.4/V14-20: the root workspace target dir and
# Cargo.lock apply when building from the repo root. These entries keep a
# stray in-crate `cargo` invocation from adding untracked artifacts.
/target/
/Cargo.lock
