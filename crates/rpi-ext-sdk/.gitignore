# Standalone crate (wasm32 target set) — build artifacts are not committed; by library-crate
# convention this crate does not commit its own Cargo.lock (serde/serde_json versions are
# pinned in Cargo.toml).
/target/
/Cargo.lock
