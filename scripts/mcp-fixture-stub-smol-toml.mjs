// Stub for `smol-toml` (see mcp-fixture-hooks.mjs): TOML host-config import
// is P2 scope, so no generated fixture may depend on it.

export function parse() {
  throw new Error("smol-toml is stubbed for fixture generation (TOML import is P2 scope)");
}
