{
  "//": "Discovery fixture materialization (R7.1.3.2/.3, TE15). Committed files under agents-tree/ cover malformed definitions, nested trees and `sync-backups` pruning. `.rpi/` and symlinks are created at test time instead of being committed: `.rpi/` is gitignored repo-wide (`.gitignore` -> `.rpi/`) and symlinks are not portable to Windows checkouts.",
  "base_dir": "agents-tree",
  "create_dirs": [".rpi"],
  "create_files": [
    {
      "path": ".rpi/pruned.md",
      "content": "---\nname: hidden-rpi-agent\ndescription: Must be pruned by discovery (R7.1.3.2 `.rpi`).\ntools: read\n---\n\nThis file exists only to prove the `.rpi` directory is pruned.\n"
    }
  ],
  "symlinks": [
    {
      "path": "linked-nested",
      "target": "nested",
      "kind": "dir",
      "expect": "followed as a directory; agents under nested/ resolve once (R7.1.3.3 #1505/#1510)"
    },
    {
      "path": "cycle",
      "target": ".",
      "kind": "dir-cycle",
      "expect": "realpath visited-set stops recursion; discovery terminates (R7.1.3.3)"
    }
  ],
  "expected_visible_agents": [
    "good-agent",
    "good-nested"
  ],
  "expected_pruned_paths": [
    ".rpi/pruned.md",
    "sync-backups/pruned.md"
  ],
  "expected_diagnostics": [
    "broken-frontmatter.md (unterminated frontmatter)",
    "broken-no-colon.md (malformed frontmatter line)"
  ]
}
