# v0.1.4-rc.13(2026-09-14)

**预发布(pre-release channel)**:`0.1.4` 正式版候选第十三轮。本轮 = **模型目录刷新至 2026-09-14 快照**(catalog-only,行为 pin 不动,偏离 D-101)。安装/升级:已装 rpi 用户 `rpi update --rc`(或 `rpi update --all --rc` 同步扩展到 `0.1.4-rc.13`);官方脚本强装 `RPI_GITHUB_API_URL=https://api.github.com/repos/revpidev/rpi/releases/tags/v0.1.4-rc.13`。

## 相对 rc.12 的增量

- **内置聊天模型目录刷新**(39 文件,1354 → **1397** 模型,+79/−36):在一次性上游 worktree(`external/pi` origin/main @ `71dca871b`)运行上游生成器拉取 models.dev/OpenRouter/NVIDIA/Vercel 快照(2026-09-14),经 `scripts/refresh-model-catalog.sh` vendor(逐文件 sha256 对 manifest 校验)。要点:Bedrock 区域族 +34(GPT-6 Astra/Grok-4.6/Gemma-4/Nova 区域前缀全量);OpenAI Codex 退役 `gpt-5.4`/`gpt-5.4-mini`;DeepSeek `v4-flash(-vision-exp)` 合并为 `deepseek-flash`;moonshotai(中/国)退役旧 kimi-k2 预览族;Fireworks +3 与 compat 更新(`allowEmptySignature`/`supportsToolReferences` 烘焙);Copilot `gpt-6-astra` 改道 Responses API;OpenRouter 366→379(批量 `:batch` 变体 + GPT Luna/Sol/Terra/Astra 别名族)。
- **图片模型目录刷新**(50 → **54**):`microsoft/mai-image-2.6`、`mai-image-2.6-flash`、`openai/gpt-image-2.5-flare`、`gpt-image-2.5-sunburst` 新增,models.dev 显示名/价格字段级更新;按 D-037 手工转录面全量重写,并新增 **vendored 参照 TS**(`crates/rpi-ai/src/images/image-models.generated.ts`)——对拍测试改读该文件(对齐 model_catalog.rs V14-09「pin submodule 生成数据在 catalog-only 刷新后有意陈旧」先例,消除 submodule 检出硬依赖)。
- **行为面零变化**:adapter 行为/请求组装/compat 消费逻辑不动;上游行为 pin 保持 `9841914`(UPSTREAM.md),catalog-only 先行偏离登记 **D-101**(v0.1.5 pin 追平 ≥`71dca871b` 后自然收敛)。
- **测试断言随目录真值更新**:总数 1397(roundtrip/generated)/54(image parity);per-provider 计数(deepseek 3→2、fireworks 20→23、huggingface 71→75、together 21→22、baseten 20→21);`gpt-5.4` 断言迁移 `gpt-5.5`(随上游 2e6fe2f98);Fireworks kimi-k2p6 compat 两键补齐;generatedAt 钉死 `2026-09-14T02:27:28.134Z`。全量门禁零失败(6280 用例)。
- **rpi-pages 远程目录同步**:`api/models/providers/*.json` 随 vendored 数据重生成(数据面,与端点无关;stable/RC 端点不触碰)。

## 验证请求(rc.13 重点)

- `rpi --list-models` 与模型选择器应显示新模型(Bedrock 区域族、GPT-6 Astra、GLM-5.3 系、DeepSeek-V4.1-Flash 等),退役模型(gpt-5.4 Codex、旧 kimi-k2 预览族)不再出现。
- 已配置 Bedrock/Copilot 的用户:新区域前缀模型可直接选用;Copilot `gpt-6-astra` 走 Responses 通道。
