# v0.1.5 (unreleased)

In progress — the main line of this release: **upstream parity** (behavioral gold standard `9841914` → `19451accd` / v0.86.1+1, ADR-0029 + ADR-0031), **plugin expansion** (rpiv-todo as the sixth first-party plugin), and **rpi-native fixes** (issues #52/#53/#54).

## Sessions and UI

- **Verified clipboard writes and headless/WSL fallbacks** (#9618/#9688, V15-11): both clipboard doors (`/copy` / `app.message.copy` and the fullscreen selection copy) now run the full write chain — platform commands first (`pbcopy` on macOS, `clip` on Windows, `termux-clipboard-set`/`wl-copy`/`xclip`/`xsel` on Linux, each bounded by a 5s timeout), then WSL interop (OSC 52 preferred inside Windows Terminal; otherwise the Windows clipboard via a UTF-8 temp file + `wslpath -w` + `powershell.exe Set-Clipboard`, which has no OSC 52 size cap), then OSC 52 with #9618/#9688 gating: remote sessions (SSH/mosh) and display-less Linux (containers, WSL without WSLg) emit OSC 52 because the terminal is the only clipboard route, while desktop sessions with a display report the failure with platform-specific guidance ("install `xclip` or `xsel` …") instead of an unverified success — oversized payloads report the 100 KB encoded limit. The fullscreen flash shows the chain's error message for 5s (success still flashes "Copied!"). Upstream's bundled native clipboard helper binaries (#9163) are not ported (deviation D-104): rpi keeps the platform-commands + OSC 52 chain, which the #9618 port completes (rpi previously wrote OSC 52 unconditionally).

- **TUI fix family** (V15-11): LaTeX legacy font switches (`{\rm …}` etc.) no longer fall back to raw source, `cases` layouts center around the surrounding equation (values padded so conditions align, middle-brace layout for even row counts), and unsupported/nested display scripts stack vertically (#8827/#9564/#7929); WezTerm fullscreen Kitty images survive later line clears (#9169 — rows are cleared before drawing when a WezTerm frame places images), and replaced tool-output images no longer render stale Kitty conversions (#8743 — cached PNG conversions carry their source bytes and are only honored while they match); file/attachment completion treats CJK punctuation (，。：等 plus full-width forms) as token boundaries without breaking CJK paths, quotes completions containing CJK punctuation, and sorts `skill:` slash commands by bare name (#9746/#9120); Alt-modified wheel scrolling is accelerated 5× (#9166); a custom footer rendering zero lines no longer reserves a blank row in fullscreen mode (#8919).

- **Per-model compaction budgets, tail-tool-result compaction, and border spinners** (#8133/#9740/`c1d4c8011`, V15-07): `compaction.modelOverrides` keys exact `"provider/modelId"` strings to per-model `reserveTokens`/`keepRecentTokens` budgets, each field resolving independently through model override → ordinary `compaction.*` setting → built-in default (16384/20000); `enabled` stays global, and global/project files deep-merge per field before lookup. Switching models re-resolves on every trigger check (constructor + each manual/auto compaction sync), and extension-visible `preparation.settings` carries the resolved values. Mid-threshold compaction no longer silently gives up when trailing tool results alone exceed `keepRecentTokens` (#9740): the cut falls back to the latest valid boundary — the assistant tool call preceding the oversized result — so the tool-call/result pair stays together and older history still summarizes. The compaction, branch-summarization, and retry spinners now embed in the editor's top border (same opt-in as the v0.1.4 working indicator): all kinds route through the editor embed first and only fall back to the standalone status row when the editor has not opted in; a thinking-level border change re-renders any active spinner.

- **Prompt cache warming** (#9668, V15-05): long tool executions and idle periods no longer silently lose expensive prompt-cache prefixes. The last session request is replayed with a one-token output cap shortly before the cache entry would expire (90% of the model's cache lifetime, ≥10s margin) whenever the expected avoided cache-miss cost exceeds the refresh cost by at least $0.05. The new `cacheWarming` setting picks `"off"` / `"streaming"` (default: warm while the agent run is active) / `"idle"` (also between runs, using a 15% continuation estimate, capped 30 min after the last real request; active runs cap at 60 min) — global-only, because each refresh costs money; `/settings` exposes it next to the HTTP idle timeout. Warming needs a known cache lifetime: built-in direct-Anthropic models carry one (`promptCache` in the catalog, V15-03), and custom models / provider overrides can declare theirs in `models.json`. Claude models with budget-based (non-adaptive) thinking are skipped while thinking is on, because the replay cannot reproduce their keyed budget. `/session` gains a Cache Warming section (mode, next decision, continuation probability, expected savings vs. the $0.05 threshold, miss penalty and refresh cost); each successful refresh shows in the transcript behind `showCacheMissNotices` with its cost. Refresh usage is persisted as a new `usage` session entry (`kind: "cache_warm"`) that counts toward session totals but never enters model context, and never appears in the conversation tree; older binaries keep reading such sessions via the unknown-entry passthrough. Extensions can veto or force each refresh through the new additive `cache_warming_decision` event (last override wins; handler failures fall back to pi's own decision).

- **Streaming renders are linear, not quadratic** (rpi#53, V15-13): long thinking/output streams no longer make the TUI progressively laggy (spinner stalls, input stutter as the message grows). Three independent fixes, all byte-identical in rendering output (snapshot suites untouched): markdown sourcepos lookups read a precomputed line-offset table instead of rescanning the document (O(lines²) → O(lines) per frame); `updateContent` diffs per-block fingerprints and keeps unchanged blocks' Markdown components (with their render caches) instead of rebuilding the whole tree per delta; and the accumulated streaming partial is shared by `Arc` end-to-end — provider stream event → agent loop → events → UI queue → message component — so per-delta processing no longer clones the whole message several times (the context tail is materialized once when the stream completes). Measured per folded delta at 100 columns (CJK thinking): 200 KB drops from ~227 ms to ~8 ms (10 KB: 3.8 → 0.45 ms; 3500-line fresh render: ~127 → ~12 ms). A `#[ignore]`d benchmark harness (`folded_delta_update_render_bench` in rpi, `render_bench` in rpi-tui) pins the three measurement scenarios.

- **Progressive session discovery** (V15-04, upstream `dfbf793b7`/`dd01f5b24`/`9b791a4cc`/`e687434a6` @ v0.86.1): the `--resume` session picker now loads progressively — the most recent sessions render as soon as their headers/messages are read (first session immediately, then batches of 10 for the current folder / 100 across all folders) instead of after a full directory scan, and selecting or cancelling a session aborts the remaining transcript reads. `--continue` stats all candidates first and reads session headers newest-first, stopping at the first cwd match (no more header-scanning the whole directory); exact `--session <id>` lookups resolve from session headers alone without loading transcript bodies. Tree navigation (`/tree`) is now rejected while a response is streaming or a compaction/branch-summary is in flight ("Wait for the current compaction or tree navigation to finish…"), so it can no longer swap the leaf out from under the compaction progress UI. Selected sessions are byte-identical to the previous implementation — only load order and timing changed.

## Tools and extensions

- **New first-party plugin: `rpiv-todo`** (sixth lockstep extension, TE34/TE35/TE36; port of `@juicesharp/rpiv-todo` v2.10.1+ @ `0fdf4f8`): the model gets a visible task list — one `todo` tool call keeps it current (six actions `create`/`update`/`list`/`get`/`delete`/`clear`, a four-state machine `pending → in_progress → completed → deleted`, incremental `blockedBy` dependency edits with cycle/foreign-key/self-blocking validation before any mutation, and a full-snapshot `details` envelope per call). The list is **rebuilt from the session branch** (last `todo` tool result wins, read through the V15-14 `ctx.sessionToolResults` ABI), so it **survives `/reload` and conversation compaction** — replayed on `session_start`/`session_compact`/`session_tree`, with per-session store slots and a first-UI foreground gate (a child session never touches the parent's panel). The panel itself is a non-focusing `aboveEditor` overlay (`● Todos (1/2)` heading, `○ ◐ ✓` glyphs, `activeForm` annotations, `⛓ #1,#2` dependencies, an overflow budget with newest-completed dropped first and a `+N more` summary, `Ctrl+O` tool-output expansion lifts the budget) plus a two-line **collapsed form** behind the `ctrl+shift+t` shortcut (`collapseKey: "off"` disables), completed-task fade-out at the next `agent_start`, and a `/todos` command with grouped status sections. Transcript rendering ships `renderCall`/`renderResult` (action glyphs `+ → × › ☰ ∅`, status echoes `○ ◐ ● ⊘`). Configuration is read-only at `~/.config/rpiv-todo/config.json` (`maxWidgetLines` default 12, `collapseKey`, `guidance` overriding the eight built-in prompt guidelines — `[VARIANT]` TE-D43, nine locale tables compiled in). Distribution is lockstep first-party: the crate joins the Release `.rpix` set (build.yml `EXT_CRATES`, sixth crate), registry key **`rpiv-todo`** at revpi.dev with `minHostVersion` = the lockstep host version (the first release rides the `v0.1.5-rc.1` RC channel — `rpi install rpiv-todo --rc`; native carrier only). The `.rpix` ship manifest is pinned by a crate-level consistency test (the port of upstream `ship-manifest.test.ts`: every production module reachable from the lib root compiles into the shipped cdylib; native-only carrier; `rpiAbi: 1`).
- **[BREAKING] Subagents: automatic model fallback removed** (pi-subagents v0.70 `#2270`/`f58dfcb5`, TE39): agent frontmatter `fallbackModels` and `subagents.agentOverrides.<name>.fallbackModels` are **no longer recognized** (ignored like any unknown field), and a subagent run never switches models mid-launch — a retryable provider failure (rate limit, 429, transient 5xx, …) now fails the child with its error, matching upstream v0.70's removal (`Retry another model only with a later explicit launch`). Migration: remove `fallbackModels` from agent definitions and overrides; wrap flaky models with an explicit retry at the call site, or list alternative models in the task so the parent can relaunch on failure. Also removed with the same machinery: the persistent model-exclusion cache (`modelExclusions.defaultTtlMs` config key now ignored) and the `attemptedModels`/`[fallback] …` retry notes in run status and artifacts.

- **Built-in tools prefer strict JSON-schema sampling** (`fcff255b0`, V15-08): `read`, `bash`, `edit`, and `write` now declare `constrainedSampling: { type: "json_schema", strict: "prefer" }` unconditionally — providers that support strict tool schemas convert the parameter schemas on their side (unsupported constructs still fall back to the unstrict form, since the preference is `prefer` not `require`), and the execution schemas themselves are unchanged. Upstream's list also includes `powershell`, which rpi does not ship. Extensions re-registering a same-named tool with `constrainedSampling: false` (or their own config) override the built-in default — the definition value now reaches the executable tool instead of being dropped at the host boundary.

- **Bash tool durations above one minute render readably** (#9628, `fe219d7f8`, V15-08): the Elapsed/Took line keeps `X.Ys` under a minute and switches to `Xm Ys` (and `Xh Ym Zs` past an hour), e.g. `1h 3m 4s` for a 2-hour-3-minute build. Sub-minute precision is unchanged, including the `59.9s → "60.0s"` rounding quirk at 59_999 ms.

- **Signal-terminated shell commands fail with their partial output** (#9577, `a8b3dd199`, V15-08): a bash child killed by a signal (OOM-killer, external `kill`, …) maps to the conventional `128 + signal` exit code and reports `Command exited with code 137`-style errors with the output collected so far, instead of silently succeeding with no exit code. Custom `BashOperations` backends resolving to no exit code at all now fail the command with `Command terminated without an exit code` (also with partial output preserved).

- **Extension tools without a parameter schema are rejected at registration** (#9300, `acaa253cc`, V15-08): `registerTool` with missing, non-object, array, or scalar `parameters` fails the registration with `Tool "<name>" registered by extension "<path>" must define an object parameter schema.` instead of breaking provider request serialization later. The wasm channel no longer silently defaults a missing schema to `{"type":"object"}`; affected guests fail to load with the same message.

- **`pi.on()` returns an unsubscribe handle** (#8967, landed as `46c9de402`, V15-09): native extensions get a callable `OnUnsubscribe` from `ExtensionApi::on` — removing exactly that registration, idempotent on repeat calls, with the last removal of an event dropping its handler entry. Handlers added or removed during a dispatch apply to later dispatches only (each dispatch snapshots the handler list; a nested dispatch takes a fresh one). Over the JSON guest ABI the same surface reads: `on` now answers `{subscriptionId}` (previously `null`; old guests ignore it) and the new additive `off` host-call (free capability, same as `on`) removes one registration — the wasm SDK wraps it as `SubscriptionId` + `subscribe`/`unsubscribe`, tearing the host forwarder down when the last guest handler of an event goes away.

- **`ctx.modelRegistry.stream()` / `streamSimple()` for extension model calls** (#8964, `1f78cea7a`, V15-09): extensions call models through the configured providers with resolved authentication (including providers registered via `pi.registerProvider()`) instead of reimplementing transport — native callers receive the live `AssistantMessageEventStream`; over the JSON guest ABI the synchronous boundary collects it into `{events, result}` (every stream event plus the final message; setup failures surface as an error event + error result, an unbound session answers the standard `unbound` envelope). Options carry the portable subset (`temperature` / `maxTokens` / `reasoning`, plus `toolChoice` on `streamSimple` where `reasoning` excludes `"off"`); the callback-bearing fields of the upstream options cannot cross JSON — the same contract `ctx.modelRegistry.complete` established.

- **Hook event/result types exported at the package entries** (#9642, `4c2d91339`, V15-09): the 60 hook event/result types and their string enums are re-exported from the `rpi_ext_host` crate root (native package entry; everything was already reachable via `types::` — surface alignment only), and the wasm SDK gains `rpi_ext_sdk::events` — typed guest-side views of the same surface for deserializing event payloads, with heavy payloads carried as `Value` per the JSON-boundary rule.

- **RPC `steer` / `follow_up` run extension `input` handlers** (#8718, `faa9863cb`, V15-09): direct RPC steering and follow-up messages now pass through the extension `input` handlers like interactive prompts, preserving their `rpc` input source and streaming behavior — a `handled` result drops the message, a `transform` result queues the transformed text (previously both commands bypassed the handlers entirely).

- **[BREAKING] `user_bash` fails closed** (#9068, `509ee2bd0`, V15-08): an extension `user_bash` handler that throws, or returns a defined-but-invalid result (anything other than exactly one valid `{ operations }` / `{ result }` object), now **aborts the `!`/`!!` command** — later handlers are not invoked and the command never falls back to the local shell. Return `undefined`/`null` to keep propagating; a full-replacement result must carry string `output`, `exitCode` (number or null), boolean `cancelled`/`truncated`, optional `fullOutputPath`. Because custom execution backends (`operations.exec` closures) cannot cross rpi's JSON extension boundary (ADR-0007 gap 1), an `operations`-shaped result is always invalid over that boundary and now aborts the command — the previous drop-with-warning fallback to local execution is gone (breaking for extensions that relied on it). The error is reported through the standard extension error surface.

- **Subagents rebase batch A: worktree admission, project memory, and background-run hygiene** (pi-subagents v0.66 → v0.70 batch-A subset, TE38): managed worktree isolation now **admission-preflights the source tree** (#2081/#2325 semantics) — a parallel batch with `worktree: true` is rejected up front with `worktree isolation requires a clean git working tree. Commit or stash changes first.` when the source repo is dirty (bookkeeping under `.rpi/subagents` is exempt), and the check repeats at allocation time; project-scoped **agent memory resolves at the project root** (nearest `.rpi`/`.agents` ancestor, git root as fallback) and a **verified linked worktree maps onto the main checkout** (#2336), so `MEMORY.md` scopes are shared across worktrees instead of fragmenting per-checkout (previously the memory root was the raw delegation cwd); worktree **`node_modules` dependency links fail closed** (#2288) — a present-but-non-directory source, a failed symlink, or a created link that does not resolve back to the source now fails the worktree setup (preserved for manual recovery) instead of being silently skipped; `resume` **rejects a stopped last child** (#2242: … child N was stopped and cannot be resumed. Start a new run instead.`) — a stopped child has no continuation to revive; status transcripts gain the **32 KiB rendered-body budget** (#2017: whole recent lines preferred, `… [earlier lines omitted]` marker) and the `lines` parameter caps at 500 (upstream value; rpi previously clamped at 1000); and the **headless agent_end drain yields** when this session owns an unanswered supervisor ask (#2202) — instead of burning up to the 30-minute drain budget on children a headless parent can never unblock, the drain returns promptly and the ask surfaces.

- **Subagents rebase batch B: allowlists, budgets, and governance** (pi-subagents v0.70 batch-B subset, TE39): agent frontmatter `allowedAgents` and `subagents.agentOverrides.<name>.allowedAgents` (#2338) restrict which canonical agent names a nesting-authorized agent may launch — inherited allowlists are intersected through the child env (`RPI_SUBAGENT_ALLOWED_AGENTS`) and can never be widened, an empty list denies every descendant launch, malformed env values fail closed to deny-all, non-allowlisted launches fail with `Capability ceiling from <sources> does not allow agent '<name>'. Allowed agents: …`, and the `list` action partitions restricted agents into their own section. The bundled reviewer gains `watchdog_diff` (#2333): a bounded read-only view of the staged/unstaged working-tree delta against its launch `HEAD` plus the untracked inventory (24_000-char budget, 50-file cap, path validation, HEAD-drift refusal — committed ranges are never shown). `toolBudget` becomes real (#2302): the config validates at admission with upstream error texts, the child extension counts every tool call (soft nudge rides a steer at the soft cap) and blocks past the hard cap for tools in the block list (default `[read, grep, find, ls]`) with the exact runtime message, and the terminal result carries the `ToolBudgetState` + `toolBudgetBlocked` when the exact blocked message is detected (pre-execution blocks read past-hard). Support surface: budgets are honored at the **call level** — the top-level `subagent` call parameter and per-task/chain-step `toolBudget` fields (per-task wins over the top level); agent-frontmatter `toolBudget` and `subagents.agentOverrides.<name>.toolBudget` are NOT wired yet (the latter warns as unsupported) — set the budget on the call site instead. New settings: `subagents.defaultSubagentOnlyExtensions` (#2322 — shared child-only extension paths for agents without their own field, ambient discovery preserved) and `subagents.agentExcludeDirs` (#2146 — discovery subtree exclusion with symlink-alias canonicalization); env `RPI_SUBAGENT_CACHE_RETENTION` (#2190) sets a child-only prompt-cache tier (ADR-0001 rename). Nested coordinators (#2087/#2272): fanout children can answer their own children's supervisor asks (`subagent_supervisor` registered in child mode, session-scoped to the coordinator's own session) and wait on their own runs (`subagent_wait`). `subagent_wait` also yields for ANY blocking ask owned by the orchestrator session (#2345 — upstream `hasPendingSupervisorRequest`), with the `supervisor_request` yield shape. Governance family: canonical agent names win over local short names with a dedicated ambiguity error (#2214); supervisor progress updates never reach the parent (filtered from the pending listing; the request file is never written — net-equal to upstream's read-then-delete, #2229/#2230); capability rows report the acceptance policy (#2213); read-only classification wins over risky topic keywords (#2191); forked children keep their requested thinking level when signed thinking blocks are stripped (#2031). Subagents pin switched `0fc0eebb` → `b72714de` (v0.70.0, ADR-0029 five-pin table; both parity tracks MATCH, argv/env golden untouched).

- **MCP adapter rebase: OAuth credential security, config editor, and the v2.34 tool surface** (pi-mcp-adapter v2.32.1 → **v2.34.0+9** `97435aab`, TE40; pin switched atomically, ADR-0029 five-pin table): a new **encrypted-file OAuth credential backend** is available for hosts without an OS credential store (Windows network logons, OpenSSH sessions) — set `settings.oauthCredentialStore: "encrypted-file"` and provide `RPI_MCP_ADAPTER_OAUTH_FILE_KEY` (canonical base64 for exactly 32 random bytes); entries live as AES-256-GCM envelopes under `<agent dir>/mcp-oauth-encrypted/` with 0700/0600 permissions, symlinked or group/other-readable files refused, and atomic tmp+fsync+rename writes (the ciphertext format follows the design ruling: NOT byte-interoperable with the upstream Node envelope). Keyring **chunking now applies only on Windows** (plus the size-limited test backend), chunk reads verify a SHA-256 digest, and an existing chunked record compacts on its next read (#565); the **logout ordering** changed — the transport closes first and a failed close keeps the credentials (#546/#563 surviving semantics). OAuth gains **Client ID Metadata Documents** (`oauth.clientMetadataUrl`, used as the client id when the server advertises CIMD support; an explicit `clientId` wins; combining the URL with `clientSecret` without a `clientId` is rejected, #571), **`{port}` loopback redirect URIs** and explicit-port exact binding with the default callback port now 19876 / `MCP_OAUTH_CALLBACK_PORT` (#483 — v0.1.4's always-OS-assigned port diverged for pre-registered clients), a stored **DCR registration is reused** across interactive flows and only dropped when its redirect URIs went stale on a non-refreshable credential (#503), **origin-scoped service headers** ride OAuth requests to the configured origin only with redirects refused (#535), and the per-request timeout is overridable via `RPI_MCP_OAUTH_REQUEST_TIMEOUT_MS` (#486; default stays rpi's stricter 10 s). The config surface adds **`/mcp edit [project|global]`** — the host external editor over `~/.config/mcp/mcp.json` or `<cwd>/.mcp.json` with symlink-safe atomic writes that preserve the target's mode (#601), **opt-in ancestor project config roots** (`settings.ancestorConfigRoots`, `~/`-expandable, HOME-contained, nearest-root selection, realpath alias dedup; only user-global configs may opt in, #556), a **comments-only optional config counts as absent** (#568), and `bearerTokenStore`/`caFile` are dropped by transport-switch overrides (#552/#539). The tool surface: `mcp({ describe, server })` resolves **server-scoped raw names** with union precedence and fail-closed ambiguity (#600, incl. the new "matches multiple tools on server" error and scoped suggestions), **`directTools: "search"`** registers schema-backed tools held out of the active set that a `mcp({ search })` activates additively (reported as `addedToolNames`; they never count toward the 75-tool advisory, #525), `mcp({ connect })` reports **`addedToolNames`** for the direct tools it discovered (consumed once per server, #502), status shows **per-server `directToolCount`** (#484), live connections **overlay the metadata cache for tool-surface syncs** so zero-TTL servers keep their tools while connected and disconnected servers retire their live catalogs (#566), **`structuredContent` coexisting with ordinary content is appended** as a `structuredContent:` block instead of being dropped (#605), oversized `details.mcpResult.structuredContent` summaries **self-identify with `omitted: true` + `preservedCount`/`droppedCount`** (#603), proxy arguments are **validated against the advertised schema before dispatch** and server-side errors no longer carry misleading schema guidance (#602), an all-disabled server table still shows its footer line (#604), **per-server HTTPS CA bundles** (`caFile`, HTTPS-only, same-origin, redirects refused, also used by the OAuth fetch, #539), and macOS **Local Network Privacy diagnostics** on unreachable literal local addresses (#544). The approval broker is now consulted **before** session grants (#536). Config-merge goldens re-recorded @ `97435aab` with new fixture cases; protocol (7/7), renderCall (24/24) and OAuth parity legs all MATCH on both the target snapshot and the post-switch default driver.

- **ask_user_question: a summary line instead of the raw-args dump** (rpi#52, TE41): the plugin now registers the existing L0 render hooks, so a questionnaire call renders one line — bold tool name + `N questions (Header, …)` — instead of pretty-printing the whole arguments JSON into the transcript; `Ctrl+O` (the existing tool-output expansion toggle) expands a per-question detail view — `  i. Header — question [multi]` plus numbered `label — description` option lines (the issue's example, minimized: no preview blocks) — and collapses back; results render state lines (`✓ N answered` / `declined (N partial)` / the error text) with per-answer detail when expanded. Streaming calls parse defensively: `argsComplete=false` counts what has arrived with a trailing `…`, and missing/malformed args degrade to the bare title line. A broken render arm can never regress to the JSON dump — panics and null answers route through the host's per-hook fallback (the pi-identical bold title); every rendered string strips control characters first so a mid-stream `\r` cannot fragment a line. Connective copy is embedded for all 9 plugin locales. Upstream registers no hooks (in pi the args never reach the transcript), so this is an rpi-native enhancement; the host's render-slot semantics are untouched, and the rpiv-mono pin moves to `0fdf4f8` (v2.10.1+1 — comment-level for this package; parity harness, locale tables and golden frames all byte-identical at the new pin).

- **Extension render hooks can no longer deadlock the TUI on the theme mutex** (found by the rpi#52 e2e, TE41): constructing a transcript tool/entry/message component passed `Arc::clone(&lock(&theme))` inline as a constructor argument — the temporary mutex guard lived for the whole constructor call, and the constructors synchronously invoke the extension render closures; a renderer reading `ctx.ui.theme()` (the only channel for multi-color single lines — ComponentTree v1 has no row) then re-entered the same mutex on the TUI thread and hung the first render forever. The clones are hoisted to their own statements so the guard drops before the constructor runs. Any `todo` tool call in a real TUI would have hit this (its render arm uses the same theme channel) — fixed ahead of the rpiv-todo lockstep e2e.

## RC verification fixes

- **rc.2** — **the rpiv-todo overlay never appeared in a real interactive session** (the tool itself worked): upstream binds extensions and the UI context atomically (`bindExtensions({ uiContext, mode: "tui" })`), but the rpi T15 port split the two and ran `bind_extensions` (which fires `session_start`) **before** attaching the interactive UI bridge — `session_start` dispatched with no UI, so the plugin's foreground claim (and with it every widget push) silently never ran. Both call sites — `InteractiveMode::init` and `rebind_session_ui` (every `/new` / `/resume` / `/clone` / `/fork` / `/import`) — now attach the bridge first. A real-mode boot e2e pins the ordering (it fails against the inverted order).

## Pre-stable review fixes

(All shipped in `v0.1.5-rc.3` — the release preview of this batch; see `changes/v0.1.5-rc.3.md`.)

- **WSL clipboard interop actually reaches PowerShell now** (V15-11 review, P0): the write-path command runner dropped the collected stdout on the floor and always answered empty bytes, so `wslpath -w` resolved to an empty Windows path and the `powershell.exe Set-Clipboard` leg never executed — real WSL (no Windows Terminal/WSLg) silently fell back to an unverified OSC 52 success, exactly what #9618 set out to eliminate. The runner now follows the upstream stdio policy: query calls (no stdin input) pipe stdout and return the real bytes; writer calls (stdin input) give the command NO output pipe at all, so a daemonizing writer (`wl-copy`) can no longer hang the drain on an inherited handle either. Pinned by real-runner regressions, including a fake-`wslpath` + recording-`powershell.exe` end-to-end chain.
- **Zero-TTL MCP servers keep their direct tools while connected** (#566 review, P0): the live-overlay cache entry carried the server's declared `ttlMs`, and `ttlMs == 0` entries are invalid by design — so a connected zero-TTL server was skipped by the direct-tool surface, contradicting the "keep their tools while connected" contract. Overlay entries now strip the declared TTL (freshness rides `cached_at` under the global max age, matching the upstream overlay); pinned by a regression test that fails when the declared TTL leaks back in.
- **RPC `bash` runs the `user_bash` extension interception** (review): the RPC-mode bash command bypassed the extension `user_bash` handler entirely (pre-existing divergence from the interactive `!`/`!!` path). The RPC leg now applies the same contract as V15-08: a full-replacement result skips local execution, a handler error or invalid result aborts the command (fail-closed — never a silent local fallback), and only an abstention continues locally. Pinned by an RPC-harness test that loads an inline `user_bash` extension and drives all three arms (red against a bare-`execute_bash` revert).
- **`mcp({ connect })` / `mcp({ search })` report `addedToolNames` at the tool-result top level** (#502/#525 review): the load-point signal previously nested under `details.addedToolNames`; it now rides the top level exactly like the upstream adapter (the `details.activated` search field stays), so cross-implementation consumers read it where they expect it. Pinned by a direct-tools e2e asserting the top-level list (and the old position gone) through the host `on_connect_report` hook.
- **Assorted review repairs** (each with a regression test unless noted): the encrypted-file OAuth key decoder no longer panics on multi-byte UTF-8 input (`éAAA`-style keys fail closed with `InvalidConfigValue`); the macOS Local Network Privacy diagnostics match the real Rust io error texts (`"No route to host"` / `"Network is unreachable"` — previously only EACCES ever matched); the approval broker-before-session-grant order (#536) is pinned by a broker-deny-over-cached-grant test; a dead DCR registration is cleared from the credential store (not just filtered in memory — the #503 gate and the `getAuthForUrl` URL scoping are each pinned by unit tests); `oauth.clientMetadataUrl` interpolates env vars and trims like `authServerMetadataUrl` (parse/validate pinned); `ask_user_question` multi-select answers strip control characters (the module's own invariant); the post-login default models for zai/zai-coding-cn/cerebras/xai match the regenerated catalog (a new test pins every default against it); the todo reducer's change detection compares JSON numbers numerically (`1` vs `1.0` is a no-op again, as upstream); `agent_capabilities` reports real `executable`/`restrictedCount`/`restrictionSources` under a capability ceiling and the `Acceptance role:` row wording matches upstream; the Anthropic beta set rides the `anthropic-beta` header only (no stray top-level `betas` body field); the wasm SDK's `unsubscribe` releases the state lock before the `off` host call (scope-verified by review; a re-entrant-host test harness remains future work); the subagents skill docs no longer teach the removed `fallbackModels` (pinned by an asset scan).

## Protocol and providers

- **[BREAKING] Mid-conversation system messages: prompt and tool declarations ride the transcript** (#9548, `9e05370b2` @ `19451accd`, V15-06 — the largest host-side surface of this release): the system prompt and the tool loadout are no longer request-time inputs — they are declared by transcript-carried `systemMessage` entries (sections patch by name, `null` removes; `toolsAdded`/`toolsRemoved` deltas), so switching sessions/branches replays the exact prompt and tool state, compaction snapshots the declaration, and providers receive incremental tool changes instead of re-sent full lists: Anthropic native `tool_addition`/`tool_removal` blocks with a deferred placeholder that keeps the cached prefix stable (gated on both capabilities + no redefinitions), OpenAI `additional_tools`/tool-search anchoring, Kimi tool-bearing system messages, and collapse-at-entry everywhere else. The agent loop declares tool-loadout changes before each request; old sessions are untouched (nothing is synthesized while idle — the first request backfills the leading declaration as a LATER system message, pinned by a red-line test). Extension surface: `before_agent_start` carries structured `systemPromptOptions` chained across handlers, and `systemPrompt` results chain as `forceSystemPrompt` (the JSON ABI bridges upstream's in-place mutation as a key-level merge; `null` deletes). Migration for native plugin authors: the context brand is now [`TranscriptContext`] — the `Provider`/`ProviderStreams`/`StreamFn`/`stream_proxy` signatures narrowed to it (normalization boundary at `Models::stream`/`stream_simple`; context-free registrations carry no context across the host ABI), and the removed deferred-tools mechanism takes `ToolResultMessage.added_tool_names` with it — declare tool-loadout changes through the transcript or the `systemPromptOptions` chain instead.
- **Agent retry delay cap, compaction cancellation races, and the EventStream regression suite** (#8826/#9340/#9777/#9055, `c37b0e03b`/`de2de549b`/`b2602be77` @ `19451accd`, V15-10): agent-level retry backoff is now capped — the new `retry.maxAgentDelayMs` settings key (default `60000`) bounds every agent retry chain (assistant calls, prepare, and the summarization leg) via `min(baseDelayMs × 2^(attempt-1), maxAgentDelayMs)`; compaction cancellation closes two races — auto compaction stores its abort token BEFORE the `compaction_start` emit, and tree navigation away from a compacting leaf aborts by token instead of racing the completion path; and rpi's channel-backed event streams pin the upstream `event-stream.test.ts` regression suite (drain order, post-terminal push, interleaved arrival, waiter registration order, end wakes all waiters) plus 10k/100k-linearity assertions — rpi's mpsc dequeue is already O(1), so the upstream `Array.shift()` quadratic drain cannot occur here.

- **Meta provider with Muse subscription OAuth** (#9096, V15-15): the Meta Model API joins the built-in provider family (`/login meta` → *Sign in with Meta*, or `META_API_KEY`) — device authorization against `auth.meta.com` mints a Model API key via the Muse Code key-mint endpoint (models default to `muse-spark-1.3`, OpenAI-Responses wire, catalog shipped with V15-03). The identity token is stored as the refresh credential and the minted key as the access credential, so the standard OAuth scheduler re-mints the key about once a day; the identity itself is not renewable, so a mint 401/403 clears the path to `/login meta` (sign in again) instead of silently retrying.

- **Built-in model catalog regenerated at the v0.86.1 pin** (`19451accd`, V15-03): 39 → 41 provider catalogs / 1397 → 1443 models (models.dev + OpenRouter/NVIDIA/Vercel snapshot 2026-09-20), retiring the v0.1.4-rc.13 catalog-only refresh (D-101 closed — catalog and behavior pin re-aligned). New faces: the **Radius public catalog** ships built-in (27 models incl. the `balanced`/`cheap`/`precise` routers — Radius models are now selectable immediately after login, with the account's gateway catalog overlaying the static baseline once fetched; custom gateways declared in `models.json` keep their own catalog and do not inherit the public one), the **Meta provider catalog** (5 Muse entries, provider + subscription OAuth land separately), per-tier **prompt-cache lifetimes** on Anthropic models (`promptCache`, consumed by the upcoming cache warming), and Cerebras models marked `supportsStrictMode: false` (mixed strict/unstrict tool usage 400s there). Retired: GPT-5.4 Codex models, the standalone `deferredToolsMode`/`supportsToolReferences` catalog keys (replaced by the mid-conversation compat faces), and stale zai-coding-cn/moonshotai entries per the current models.dev snapshot.
- **SSE idle timeout now defaults to unlimited** (rpi#54, V15-12, deviation D-103 from pi/undici's 5-minute default): `DEFAULT_HTTP_IDLE_TIMEOUT_MS` flips from `300_000` to `0` (disabled), so rpi itself never kills a streaming request over a silent gap by default — reasoning-model thinking pauses, first-token latency after a large prefill, and buffering gateways frequently sit silent for more than five minutes, and the old default aborted those perfectly healthy streams mid-thought (`stream idle timeout: no data for Xms`). The three idle budgets (connect / headers wait / inter-chunk body gap, V13-08 semantics) and explicit configuration are unchanged; the `/settings` selector renders the default as `unlimited`. **Restoring the old behavior**: set `httpIdleTimeoutMs: 300000` in `~/.rpi/agent/settings.json` (any finite value re-enables the idle kill at that budget); genuinely dead peers are still surfaced by OS/TCP-level errors, and Esc-abort remains immediate regardless of the timeout.
